import React, { memo } from 'react';
import styled from '@emotion/native';

const Container = styled.View({
  flex: 1,
});

const NowPlayingItem = () => {
  return <Container></Container>;
};

export default memo(NowPlayingItem);
