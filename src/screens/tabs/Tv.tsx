import React, { memo } from 'react';
import { SafeAreaView, Text } from 'react-native';

const Tv = () => {
  return (
    <SafeAreaView>
      <Text>Tv</Text>
    </SafeAreaView>
  );
};

export default memo(Tv);
