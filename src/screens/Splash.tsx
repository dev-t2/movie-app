import React, { memo } from 'react';
import { SafeAreaView, Text } from 'react-native';

const Splash = () => {
  return (
    <SafeAreaView>
      <Text>Splash</Text>
    </SafeAreaView>
  );
};

export default memo(Splash);
